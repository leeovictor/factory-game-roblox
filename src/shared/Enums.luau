local ProductionFacilityState = {
	Idle = "idle",
	Producing = "producing",
	LackMaterial = "lack_material",
	ProductOverflow = "product_overflow",
}

return {
	ProductionFacilityState = ProductionFacilityState,
}
